apiVersion: v1
kind: Service
metadata:
  name: {{ include "finetuning-engine.fullname" . }}
  labels:
    {{- include "finetuning-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.app.service.type }}
  ports:
    - port: {{ .Values.app.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finetuning-engine.selectorLabels" . | nindent 4 }}
