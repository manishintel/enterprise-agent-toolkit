apiVersion: v1
kind: Secret
metadata:
  name: {{ include "finetuning-engine.fullname" . }}-secret
  labels:
    {{- include "finetuning-engine.labels" . | nindent 4 }}
type: Opaque
stringData:
  database-url: {{ required "secrets.databaseUrl is required" .Values.secrets.databaseUrl | quote }}
