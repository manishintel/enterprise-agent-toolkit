# Runtime logs
deploy.log

# Kubespray clone (large, fetched at deploy time)
core/kubespray/

# Vault secrets
core/inventory/metadata/vault.yml
core/inventory/metadata/all.yml

# Auto-generated secrets (contain hashed/plaintext passwords — never commit)
docker/redis/acl.conf
docker/postgres/init.sql
docker/vault.yml

# Auto-generated compose files (built from config.cfg + model-configs.yaml at deploy time)
docker/docker-compose.vllm.yml
docker/.env

# Never commit local configs or secrets
*.pem
*.key
*.crt
*_secret*
*.env
blueprints/finetuning_service/finetune-config.cfg
